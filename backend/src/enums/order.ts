export enum ORDER_STATUS {
    INPROCESS = 'INPROCESS',
    ACCEPTED = 'ACCEPTED',
    REJECTED = 'REJECTED',
}

export enum ORDER_STAGE {
    ONBOARD = 'INPROCESS',
    INWAY = 'INWAY',
    DELIVERED = 'DELIVERED',
}